VanillaTilt.init(document.querySelectorAll(".person"), {
    max: 10,
    speed: 1200,
    glare: true,
    "max-glare": 0.2,

    transition: true,
    easing: "cubic-bezier(.03,.98,.52,.99)",
    scale: 1.1,
});

VanillaTilt.init(document.querySelectorAll(".subject"), {
    max: 10,
    speed: 1200,
    glare: true,
    "max-glare": 0.2,

    transition: true,
    easing: "cubic-bezier(.03,.98,.52,.99)",
    scale: 1.1,
});